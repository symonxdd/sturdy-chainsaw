{
  "name": "wails-release-helper",
  "scripts": {
    "release:patch": "npm --prefix frontend version patch && git push && git push --follow-tags",
    "release:minor": "npm --prefix frontend version minor && git push && git push --follow-tags",
    "release:major": "npm --prefix frontend version major && git push && git push --follow-tags"
  }
}